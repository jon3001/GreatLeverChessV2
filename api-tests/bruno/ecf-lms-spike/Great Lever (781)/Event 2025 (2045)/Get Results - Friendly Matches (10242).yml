info:
  name: Get Results - Friendly Matches (10242)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/event/10242/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
