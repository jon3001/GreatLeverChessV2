info:
  name: Get Results - Div B League (9132)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/event/9132/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
