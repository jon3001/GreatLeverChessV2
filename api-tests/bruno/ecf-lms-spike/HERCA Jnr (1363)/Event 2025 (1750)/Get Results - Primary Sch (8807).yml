info:
  name: Get Results - Primary Sch (8807)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/event/8807/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
