info:
  name: Get Results - Div B League (6827)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/event/6827/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
