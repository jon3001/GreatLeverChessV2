info:
  name: Get Events
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/season/1805/events"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
