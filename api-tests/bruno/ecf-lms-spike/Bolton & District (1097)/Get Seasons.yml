info:
  name: Get Seasons
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/org/1097/seasons"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
