info:
  name: Get Results - Div 2 League (8867)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/event/8868/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
