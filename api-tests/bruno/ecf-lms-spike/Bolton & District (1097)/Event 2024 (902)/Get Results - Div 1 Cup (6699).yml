info:
  name: Get Results - Div 1 Cup (6699)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/event/6699/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
