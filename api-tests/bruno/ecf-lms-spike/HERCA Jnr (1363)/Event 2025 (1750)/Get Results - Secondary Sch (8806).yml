info:
  name: Get Results - Secondary Sch (8806)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/event/8806/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
