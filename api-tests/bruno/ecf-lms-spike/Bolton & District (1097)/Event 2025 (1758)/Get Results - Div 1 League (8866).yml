info:
  name: Get Results - Div 1 League (8866)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/event/8868/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
