info:
  name: Get Results - Div B Cup (6829) Vaux Cup
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/event/6829/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
