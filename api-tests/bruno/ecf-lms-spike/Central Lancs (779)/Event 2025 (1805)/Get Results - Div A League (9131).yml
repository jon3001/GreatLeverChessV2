info:
  name: Get Results - Div A League (9131)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/event/9131/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
