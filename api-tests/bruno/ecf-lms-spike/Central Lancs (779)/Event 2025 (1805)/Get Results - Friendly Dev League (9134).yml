info:
  name: Get Results - Friendly Dev League (9134)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/event/9134/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
