info:
  name: Get Seasons
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/org/1237/seasons"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
