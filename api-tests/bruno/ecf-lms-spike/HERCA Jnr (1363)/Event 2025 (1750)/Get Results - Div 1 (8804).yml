info:
  name: Get Results - Div 1 (8804)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/event/8804/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
