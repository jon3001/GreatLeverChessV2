info:
  name: Get Results - Div 2 Cup (6702)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/event/6702/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
