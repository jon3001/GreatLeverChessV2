info:
  name: Get Results - Friendly Dev League (6842)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/event/6842/results"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
